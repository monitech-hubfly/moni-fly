node_modules/
.next/
dist/
.env
.env.local
.env.*.local

# Supabase CLI (cache local; não versionar)
supabase/.temp/

# Artefacto incremental do TypeScript
*.tsbuildinfo

# Duplicata local do utilitário TypeScript (usar scripts/test-bastoes-prod.ts)
scripts/test-bastoes-prod.mjs

# Instruções locais para o Claude Code (não versionar)
CLAUDE.md