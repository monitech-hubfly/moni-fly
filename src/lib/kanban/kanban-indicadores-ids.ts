/**
 * IDs dos indicadores de KANBAN que são do tipo 'mensal_acumulado'.
 * Arquivo puro (sem imports server-side) — pode ser importado tanto por
 * client hooks quanto por cron jobs server-side.
 */
export const KANBAN_INDICADORES_MENSAIS_IDS: ReadonlySet<string> = new Set([
  '34ea3769-d0c9-4c89-8710-386f48f830a6', // Contratos Assinados Portfólio
]);
