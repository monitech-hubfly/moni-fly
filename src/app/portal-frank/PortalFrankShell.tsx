'use client';

import type { ReactNode } from 'react';

/** Cabeçalho fixo com « Hub Fly », título e Sair vem do AppShell (`AppStickyHeader`). */
export function PortalFrankShell({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
